// Single entry point for the shared package.
// Import everything from here — never import directly from sub-files.
// Usage: import { Role, PaperStatus, IUser } from '@journal/shared'

export * from "./types/user.types";
export * from "./types/paper.types";
export * from "./types/review.types";
