{
  "name": "scientific-journal",
  "private": true,
  "workspaces": [
    "apps/*",
    "apps/packages/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:backend": "turbo run dev --filter=backend",
    "dev:frontend": "turbo run dev --filter=frontend"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "@journal/shared": "^1.0.0"
  }
}
